const homePath = '/phonebook';

export const appRoutes = {
  HOME: `${homePath}/`,
  LOGIN: `${homePath}/login`,
  REGISTER: `${homePath}/register`,
  USER_PHONE_BOOK: `${homePath}/user-phone-book`,
};
